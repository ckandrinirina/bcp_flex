<aside class="main-sidebar">

    <!-- sidebar: style can be found in sidebar.less -->
    <section class="sidebar">
        <!-- Sidebar Menu -->
        <ul class="sidebar-menu" data-widget="tree">
            <li class="header">MENU</li>
            <!-- Optionally, you can add icons to the links -->
            <li {% if menu == 'event' %}class="active"{% endif %}>
                <a href="{{path('event_index')}}">
                    <i class="fa fa-link"></i>
                    <span>Evenement</span>
                </a>
            </li>
            <li {% if menu == 'recette' %}class="active"{% endif %}>
                <a href="{{path('recette_index')}}">
                    <i class="fa fa-link"></i>
                    <span>Recette</span>
                </a>
            </li>
            <li {% if menu == 'restaurant' %}class="active"{% endif %}>
                <a href="{{path('restaurant_index')}}">
                    <i class="fa fa-link"></i>
                    <span>Restaurant</span>
                </a>
            </li>
            <li {% if menu == 'hotel' %}class="active"{% endif %}>
                <a href="{{path('hotel_index')}}">
                    <i class="fa fa-link"></i>
                    <span>Hotel</span>
                </a>
            </li>
            {# <li class="treeview">
                <a href="#">
                    <i class="fa fa-link"></i>
                    <span>Multilevel</span>
                    <span class="pull-right-container">
                        <i class="fa fa-angle-left pull-right"></i>
                    </span>
                </a>
                <ul class="treeview-menu">
                    <li>
                        <a href="#">Link in level 2</a>
                    </li>
                    <li>
                        <a href="#">Link in level 2</a>
                    </li>
                </ul>
            </li> #}
        </ul>
        <!-- /.sidebar-menu -->
    </section>
    <!-- /.sidebar -->
</aside>