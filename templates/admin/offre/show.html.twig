{% extends 'admin/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/admin/css/app.css') }}">
    <style>
        .wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
{% endblock %}
{% block body %}
    <nav class="navbar">
        <a class="navbar-brand"></a>
        <h2>details offre </h2>
        <form class="form-inline">
            <a href="{{ path('admin_offre_index') }}" class="btn btn-primary navbar-brand">Retour à la liste</a>
        </form>
    </nav>

    <div class="wrapper">
        <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ offre.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ offre.prix }}</td>
            </tr>
            <tr>
                <th>Descriptions</th>
                <td>{{ offre.descriptions }}</td>
            </tr>
            <tr>
                <th>Idetablissement</th>
                <td>{{ offre.idEtablissement }}</td>
            </tr>
        </tbody>
    </table>
            <table>
            <td>
                
            </td>
            <td>
                <a href="{{ path('admin_offre_edit', { 'id': offre.id }) }}" class="btn btn-primary navbar-brand">Editer</a>
            </td>
            <td>
                {{ include('admin/_delete_form.html.twig') }}
            </td>
        </table>
{% endblock %}
