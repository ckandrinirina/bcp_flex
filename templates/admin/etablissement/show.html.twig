{% extends 'admin/base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('assets/admin/css/app.css') }}">
    <style>
        .wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
{% endblock %}
{% block body %}
    <nav class="navbar">
        <a class="navbar-brand"></a>
        <h2>details Etablissement </h2>
        <form class="form-inline">
            <a href="{{ path('etablissement_index') }}" class="btn btn-primary navbar-brand">Retour à la liste</a>
        </form>
    </nav>

    <div   class="wrapper">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ etablissement.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ etablissement.nom }}</td>
                </tr>
                <tr>
                    <th>Adresse</th>
                    <td>{{ etablissement.adresse }}</td>
                </tr>
                <tr>
                    <th>Fixe</th>
                    <td>{{ etablissement.fixe }}</td>
                </tr>
                <tr>
                    <th>Autre</th>
                    <td>{{ etablissement.autre }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ etablissement.email }}</td>
                </tr>
                <tr>
                    <th>Site</th>
                    <td>{{ etablissement.site }}</td>
                </tr>
                <tr>
                    <th>Specialite</th>
                    <td>{{ etablissement.specialite }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ etablissement.prix }}</td>
                </tr>
                <tr>
                    <th>description</th>
                    <td>{{ etablissement.description }}</td>
                </tr>
                <tr>
                    <th>nombre de vue</th>
                    <td>{{ etablissement.viewers }}</td>
                </tr>
            </tbody>
        </table>

        <table>
            <td>
                
            </td>
            <td>
                <a href="{{ path('etablissement_edit', { 'id': etablissement.id }) }}" class="btn btn-primary navbar-brand">Edit</a>
            </td>
            <td>
                {{ include('admin/_delete_form.html.twig') }}
            </td>
        </table>
    </div>
{% endblock %}
