<header class="navbar navbar-inverse bs-docs-nav" role="banner">
	<div class="container">
		<nav class="collapse navbar-collapse bs-navbar-collapse" role="navigation">
			<ul class="nav navbar-nav navbar-right">
				<li>
					<a href="{{path('home')}}">Accueil</a>
				</li>
				<li {% if type == 'restaurant' %} class="active" {% endif %}>
					<a href="{{path('search_detail',{ 'type':'restaurant' })}}">Restaurant et Snack</a>
				</li>
				<li {% if type == 'hotel' %} class="active" {% endif %}>
					<a href="{{path('search_detail',{ 'type':'hotel' })}}">Hotels</a>
				</li>
				<li class="dropdown {% if type == 'recette' or type == 'event' %}active{% endif %}">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">Autres
						<b class="caret"></b>
					</a>
					<ul class="dropdown-menu">
						<li>
							<a href="{{path('search_detail',{ 'type':'event' })}}">Evènement</a>
						</li>
						<li>
							<a href="{{path('search_detail',{ 'type':'recette' })}}">Recette</a>
						</li>
					</ul>
				</li>
				<li>
					<a href="{{path('contact')}}">Contact</a>
				</li>
			</ul>
		</nav>
	</div>
</header>
